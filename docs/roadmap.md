# Roadmap

The roadmap and current implementation status now live together in `../PROGRESS.md`.

Use `PROGRESS.md` for:

- current milestone status
- completed work
- `V2`, `V3`, and `V4` planning
- validation status
- next actions
